doctype html
html
   head
      title Found Me
      link(rel='stylesheet', type='text/css', href='https://bootswatch.com/4/sketchy/bootstrap.css')
   body
      nav.navbar.navbar-expand-lg.navbar-dark.bg-primary
         a.navbar-brand(href='/') USER Info
         button.navbar-toggler(type='button', data-toggle='collapse', data-target='#navbarColor01', aria-controls='navbarColor01', aria-expanded='false', aria-label='Toggle navigation')
            span.navbar-toggler-icon
         #navbarColor01.collapse.navbar-collapse
            ul.navbar-nav.mr-auto
               li.nav-item.active
                  a.nav-link(href='/')
                     | Home 
               span.sr-only (current)
               li.nav-item
                  a.nav-link(href='/search') Search
               li.nav-item
                  a.nav-link(href='/addMe') Add me
      h1 USERS
      if results 
         table.table.table-striped.table-hover.table-bordered
            thead.thead-dark
               tr
                  th First name
                  th Last name
                  th Email
            tbody
               tr
                  td #{JSON.stringify(results.firstname)}
                  td #{JSON.stringify(results.lastname)}
                  td #{JSON.stringify(results.email)}
      //-    table
      //-       tr
      //-          th.header First name
      //-          th.header Last name
      //-          th.header Email
      //-       tr
      //-          th.index #{JSON.stringify(results.firstname)}
      //-          th.index #{JSON.stringify(results.lastname)}
      //-          th.index #{JSON.stringify(results.email)}
      //- else 
      //-    p No match
         
      .footer
         p Copyright © 2017 by Vincent K
